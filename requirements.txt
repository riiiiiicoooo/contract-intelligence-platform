# Contract Intelligence Platform - Python Dependencies
# Last Updated: March 2026

# ============================================================================
# CORE DEPENDENCIES
# ============================================================================

# LLM & AI
anthropic==0.28.0              # Claude API client (primary model)
openai==1.35.0                 # GPT-4 fallback (secondary model)
langchain==0.1.17              # LLM orchestration
langgraph==0.1.0               # State machine for workflow

# Embeddings
voyage-ai==0.2.0               # Voyage Law-2 semantic embeddings (legal domain)

# Document Processing
docling==0.20.0                # Native PDF parsing (DocLayNet + TableFormer)
python-docx==0.8.11            # Word document processing
pdf2image==1.16.0              # PDF to image conversion for OCR
pillow==10.0.0                 # Image processing

# Vector Database
psycopg2-binary==2.9.9         # PostgreSQL adapter
pgvector==0.2.4                # pgvector Python interface
sqlalchemy==2.0.36             # ORM for database operations
alembic==1.14.0                # Database migrations

# API & Server
fastapi==0.115.0               # REST API framework
pydantic==2.10.0               # Data validation with Python types
pydantic-settings==2.6.0       # Environment-based settings
uvicorn==0.30.0                # ASGI server
starlette==0.40.0              # Web framework

# Authentication & Security
supabase==2.0.0                # Supabase Python client
pyjwt==2.8.0                   # JWT token handling
bcrypt==4.1.1                  # Password hashing

# PII & Compliance
presidio-analyzer==2.2.354      # Microsoft Presidio for PII redaction
presidio-anonymizer==2.2.354

# Data Export
openpyxl==3.1.2                # Excel file generation
python-pptx==0.6.23            # PowerPoint generation
weasyprint==59.0               # PDF report generation from HTML
jinja2==3.1.2                  # Template engine for reports

# Async & Parallelization
celery==5.3.4                  # Distributed task queue for exports
redis==5.2.0                   # Cache & message broker
python-dateutil==2.8.2         # Date/time utilities

# Search & Retrieval
cohere==4.34.0                 # Cohere Rerank API for search results

# Resilience
tenacity==8.2.3                # Retry with exponential backoff
pybreaker==1.2.0               # Circuit breaker pattern

# Monitoring & Logging
python-json-logger==2.0.7      # JSON structured logging
sentry-sdk==1.38.0             # Error tracking

# ============================================================================
# DEVELOPMENT DEPENDENCIES
# ============================================================================

# Testing
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==5.0.0
factory-boy==3.3.0             # Test data generation
faker==21.0.0                  # Fake data generation

# Linting & Formatting
ruff==0.8.0                    # All-in-one linter & formatter (replaces black, isort, flake8)
mypy==1.13.0                   # Type checker

# Documentation
sphinx==7.2.6                  # Documentation generator
sphinx-rtd-theme==2.0.0        # ReadTheDocs theme

# Development Tools
ipython==8.17.2                # Interactive Python shell
ipdb==0.13.13                  # IPython debugger
python-dotenv==1.0.0           # Load .env files
click==8.1.7                   # CLI framework

# ============================================================================
# OPTIONAL DEPENDENCIES
# ============================================================================

# Azure OCR (alternative to Docling for scanned documents)
# azure-cognitiveservices-vision-computervision==0.12.0
# msrest==0.7.1

# AWS S3 Integration (if using AWS instead of Supabase)
# boto3==1.34.0
# botocore==1.31.0

# Google Cloud (alternative infrastructure)
# google-cloud-storage==2.10.0
# google-cloud-bigquery==3.14.0

# Temporal.io (durable pipelines - optional, use if more complex async needed)
# temporalio==1.3.0

# ============================================================================
# VERSIONS & NOTES
# ============================================================================

# Python 3.10+ required (uses native type hints, pattern matching)
# Last tested: Python 3.11.7

# Breaking changes to watch:
# - Anthropic: check release notes for API changes
# - LangChain: rapidly evolving, pin to tested version
# - pgvector: schema-dependent, coordinate with DB migrations
# - Pydantic v2: breaking changes from v1, ensure compatibility
